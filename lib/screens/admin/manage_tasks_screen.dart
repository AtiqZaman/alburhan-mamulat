// ============================================================
// FILE: lib/screens/admin/manage_tasks_screen.dart
// COPY THIS CODE INTO: lib/screens/admin/manage_tasks_screen.dart
// ============================================================

import 'package:flutter/material.dart';

class ManageTasksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('معمولات منیج کریں')),
      body: Center(
        child: Text('معمولات منیج کریں - جلد آ رہی ہے'),
      ),
    );
  }
}