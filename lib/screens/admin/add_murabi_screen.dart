// ============================================================
// FILE: lib/screens/admin/add_murabi_screen.dart
// COPY THIS CODE INTO: lib/screens/admin/add_murabi_screen.dart
// ============================================================

import 'package:flutter/material.dart';

class AddMurabiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('مربی شامل کریں')),
      body: Center(
        child: Text('مربی شامل کریں - جلد آ رہی ہے'),
      ),
    );
  }
}