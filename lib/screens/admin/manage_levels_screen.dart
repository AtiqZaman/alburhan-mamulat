// ============================================================
// FILE: lib/screens/admin/manage_levels_screen.dart
// COPY THIS CODE INTO: lib/screens/admin/manage_levels_screen.dart
// ============================================================

import 'package:flutter/material.dart';

class ManageLevelsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('سطحیں منیج کریں')),
      body: Center(
        child: Text('سطحیں منیج کریں - جلد آ رہی ہے'),
      ),
    );
  }
}