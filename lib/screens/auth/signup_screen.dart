// ============================================================
// FILE: lib/screens/auth/signup_screen.dart
// COPY THIS CODE INTO: lib/screens/auth/signup_screen.dart
// ============================================================

import 'package:flutter/material.dart';

class SignupScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('سائن اپ')),
      body: Center(
        child: Text('سائن اپ اسکرین - جلد آ رہی ہے'),
      ),
    );
  }
}